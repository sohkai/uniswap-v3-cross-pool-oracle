{
  "name": "uniswap-v3-cross-pool-oracle",
  "private": true,
  "version": "0.0.1",
  "license": "GPL-2.0-or-later",
  "engines": {
    "node": ">=10"
  },
  "devDependencies": {
  },
  "dependencies": {
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
